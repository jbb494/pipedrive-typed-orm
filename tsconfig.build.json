{
  "extends": "./tsconfig.base.json",
  "exclude": ["test"],
  "compilerOptions": {
    "outDir": "./dist" /* Specify an output folder for all emitted files. */,
    "declaration": true,
    "rootDir": "./src"
  }
}
